# DOX -- MyProfile/

**Legacy profile page (HTML + CSS + JS) for the founder**

## Purpose

Self-contained static page built with vanilla HTML/CSS/JS. No React, no Next.js routing. Reached from the main site via legacy routing.

## Local Contracts

- Plain HTML5 with linked CSS (`style.css`) and JS (`script.js`)
- No build step, no transpilation
- Favicon served as JPEG (unusual — keep as-is unless migrating)
