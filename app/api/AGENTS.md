# DOX -- app/api/

**API route handlers (REST endpoints)**

Server-only endpoint handlers. Used for forms, lead capture, and third-party integrations. Each handler exports named HTTP method functions (GET, POST, etc).
